"""Pretraining package: flavor registry, losses, and the vendored CheMeleon pipeline.

The training scripts (train.py, split.py) run as scripts from this directory and use
top-level imports; flavors.py is import-safe from the repo root as pretraining.flavors.
"""
