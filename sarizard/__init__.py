"""SARizard: fit-to-purpose CheMeleon foundations.

Subpackages: analysis (report card, meta-model), pretraining (vendored pipeline and
target calculators), corpus (shared-corpus prep), configs (recipe generation).
"""
